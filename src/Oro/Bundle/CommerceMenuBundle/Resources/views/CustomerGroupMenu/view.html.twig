{% extends '@OroCommerceMenu/menuUpdate/view.html.twig' %}

{% set routePrefix = 'oro_commerce_menu_customer_group_menu_' %}

{% oro_title_set({params : {"%customerGroup%": customerGroup.name, "%title%": entity.name}}) %}

{% set breadcrumbs = {
    'entity': entity,
    'indexPath': path('oro_customer_customer_group_index'),
    'indexLabel': 'oro.customer.customergroup.entity_plural_label'|trans,
    'additional': [
        {
            'indexPath': path('oro_customer_customer_group_view', {id: customerGroup.id}),
            'indexLabel': customerGroup.name
        },
        {
            'indexPath': path('oro_commerce_menu_customer_group_menu_index', {id: customerGroup.id}),
            'indexLabel': 'oro.commercemenu.menuupdate.roots_plural_label'|trans
        }
    ],
    'entityTitle': entity.name
} %}
