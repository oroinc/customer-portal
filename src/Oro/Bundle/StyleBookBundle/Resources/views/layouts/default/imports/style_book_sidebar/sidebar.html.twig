{% block __style_book_sidebar__container_widget %}
    {% if block.count > 0 %}
        {% set attr = layout_attr_defaults(attr, {
            '~class': ' style-book-sidebar sticky sticky--top',
            'data-role': 'style-book-sidebar',
        }) %}

        <aside {{ block('block_attributes') }}>
            {{ block_widget(block) }}
        </aside>
    {% endif %}
{% endblock %}
