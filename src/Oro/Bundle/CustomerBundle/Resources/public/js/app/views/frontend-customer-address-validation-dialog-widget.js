import ParentDialogWidget from 'oroaddressvalidation/js/app/views/frontend-address-validation-dialog-widget';

const FrontendCustomerAddressValidationDialogWidget = ParentDialogWidget.extend({});

export default FrontendCustomerAddressValidationDialogWidget;
