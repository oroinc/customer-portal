<li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
        {% if isMobileVersion() %}
            <i class="fa-user"></i>
        {% else %}
            {{ app.user|oro_format_name }}
            <i class="fa-sort-down"></i>
        {% endif %}
    </a>
    {{ oro_menu_render('customer_usermenu') }}
</li>
