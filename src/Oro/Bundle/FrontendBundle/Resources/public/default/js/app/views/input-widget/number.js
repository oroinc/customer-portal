import NumberInputWidget from 'oroui/js/app/views/input-widget/number';

const FrontendNumberInputWidget = NumberInputWidget.extend({
    allowZero: false
});

export default FrontendNumberInputWidget;
