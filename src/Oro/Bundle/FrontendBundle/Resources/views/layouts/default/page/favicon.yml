layout:
    actions:
        - '@addTree':
            items:
                favicons:
                    blockType: favicons
                    options:
                        favicons_path: '=data["theme"].getFaviconsPath(context["theme"])'
            tree:
                head:
                    favicons: ~
